version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: go_app
    depends_on:
      - db
    ports:
      - "8080:8080"
    networks:
      - app-network

  @db

volumes:
  pgdata:

networks:
  app-network:
    driver: bridge
